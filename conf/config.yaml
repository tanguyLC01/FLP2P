defaults:
  - data: mnist
  - model: lenet5
  - partition: iid
  - client: default
  - train: default
  - graph: fiedler

seed: 42
use_cuda: true
run_name: mnist_test
hydra:
  run:
    dir: ./${run_name}/${now:%Y-%m-%d}/${now:%H-%M-%S}